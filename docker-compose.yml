version: '3'

services:
  db:
    image: postgres:latest
    volumes:
      - db-data:/var/lib/postgresql/data/
    env_file:
      - ./.env
  web:
    expose:
      - 8000
    restart: always
    volumes:
      - .:/app
      - /app/web/static:/static
    depends_on:
      - db
    env_file:
      - ./.env
    build: .
  nginx:
    build: ./nginx
    volumes:
      - /app/web/static:/static
    ports:
      - "80:80"
    depends_on:
      - web
volumes:
  db-data:
  static_volume:
  media_volume: